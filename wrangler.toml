name = "mojian-ink-atelier"
compatibility_date = "2026-07-13"
pages_build_output_dir = "dist"

# Pages Functions runtime bindings — values are injected as encrypted secrets
# via `wrangler pages secret put` or the Cloudflare dashboard. Declared here
# only so `wrangler types` emits a stable `Env` interface for functions/types.d.ts.
# Do NOT put real secret values in this file.
[vars]
NOTION_DATA_SOURCE_ID = ""
SITE = "https://blog.kiwi.moe"